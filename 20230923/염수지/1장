# 1장 Jupyter: 파이썬에 날개를 달자
  
  1) IPython과 Jupyter 시작하기
    - IPython 셸 실행하기
    - Jupyter 노트북 실행하기
    - IPython의 도움말과 문서
        ?로 문서 확인하기
          ex. help(len), len?, L 정의 후 L?
        ??로 소스코드에 접근하기
          파이썬이 아니라 다른 컴파일된 확장 언어로 구현됐으면 ?와 같은 결과가 나옴
        탭 자동 완성으로 모듈 탐색하기
          ex. 객체 내용의 탭 자동완성 = 내장함수 dir() 와 같은 효과 -> L.<TAB>, L.c<TAB>
        임포트할 때 탭 자동 완성 사용하기
          ex. from itertools import co<TAB>
        탭 완성을 넘어: 와일드카드 매칭
          ex. * -> 앞, 뒤, 앞뒤에 들어갈 수 있는 모든 객체
    - IPython 셸에서 사용할 수 있는 키보드 단축키
        텍스트 입력 단축키
          ex. ctrl-k -> 현재 커서부터 그 줄 마지막까지 잘라냄
              ctrl-u -> 그 줄의 처음부터 현재 커서까지 잘라냄
    
  2) 향상된 대화형 기능
    - IPython 매직 명령어
    - 입력/출력 이력
    - IPython과 셸 명령어

  3) 디버깅 및 프로파일링
    - 에러와 디버깅
        %xmode Plain -> 에러 내용 간략히
        %xmode Verbose -> 에러 내용 자세히
    - 디버깅: 역추적 내용을 확인하는 것으로 충분하지 않을 경우
        디버깅-> %debug
    - 코드 프로파일링 및 시간 측정
        코드 조각의 실행 시간 측정하기: %timeit과 %time
        전체 스크립트 프로파일링하기: %prun
        %Iprun으로 라인 단위 프로파일링하기
        메모리 사용 프로파일링 : %memit %mprun
    - IPython 추가 참고 자료

  
